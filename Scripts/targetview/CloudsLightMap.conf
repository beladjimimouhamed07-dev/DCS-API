dofile("Scripts//targetview//_targetViewCommon.lua")

-- sz = 64 -- view size
sz = 50*2 -- view size
-- sz = 256 -- view size

cloudMapDepth = 32

targetView.addRow(sz) -- 1
targetView.addRow(sz) -- 2
targetView.addRow(sz) -- 3
targetView.addRow(sz) -- 4


targetView.addTarget3DSlices('cloudLightMap',		1, 1, 0, cloudMapDepth, 1, 0)
targetView.addTarget3DSlices('cloudLightMap',		2, 1, 0, cloudMapDepth, 1, 1)
targetView.addTarget3DSlices('cloudLightMap',		3, 1, 0, cloudMapDepth, 1, 2)
targetView.addTarget3DSlices('cloudLightMap',		4, 1, 0, cloudMapDepth, 1, 3)
